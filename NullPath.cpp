#include<bits/stdc++.h>

using namespace std;

int main(int argc,char* argv[]){
    char* varg = argv[1];
    if(varg == "NullExcept"){
        system("cls");
        printf("Input Number Is Invalid!\n");
        system("pause");
        return 0;
    }
}