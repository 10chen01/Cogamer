#include<bits/stdc++.h>
#include "Macro.hpp"

using namespace std;

int main(int argc,char* argv[]){
    char* varg = argv[1];
    if(varg == "NullExcept"){
        system("cls");
        pr_line("X");
        printf(bold("Input Number Is Invalid!\n"));
        pr_line("X");
        system("pause");
        return 0;
    }
}